package com.example.common;

/**
 * A collection of constant values used throughout the application.
 * Provides predefined values for common configurations and keys.
 */
public interface Constants {

    /**
     * The key used to retrieve the JWT token from HTTP requests.
     */
    String TOKEN = "token";

    /**
     * The default password assigned to new user accounts.
     */
    String USER_DEFAULT_PASSWORD = "123456";
}

